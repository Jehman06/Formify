version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd form-api/frontend
    build:
      commands:
        - npm ci
        - npm run build
  artifacts:
    baseDirectory: form-api/frontend/build
    files:
      - '**/*'
  cache:
    paths:
      - 'node_modules/**/*'